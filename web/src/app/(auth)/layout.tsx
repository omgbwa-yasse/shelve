/**
 * Layout du groupe (auth) — pages d'authentification hors coquille back-office.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen">{children}</div>;
}
