/**
 * Feature Chat — services connectés à l'API (conversations D12).
 */
import { createResourceApi } from '@/lib/api/resources';

export const chatConversationsApi = createResourceApi('workplace-conversations');
