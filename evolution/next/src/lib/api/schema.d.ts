/**
 * Placeholder — ce fichier sera généré automatiquement depuis `openapi.yaml`
 * (contrat gelé en phase 1, voir PHASE-1-API-LARAVEL.md). Ne pas éditer à la
 * main une fois la génération en place :
 *
 *   npx openapi-typescript openapi.yaml -o src/lib/api/schema.d.ts
 */
export type paths = Record<string, unknown>;
export type components = { schemas: Record<string, unknown> };
